#--- Utility Methods


class Utilities():
    def __init__(self, driver):
        driver = self.driver

    def get_locator_by(separator, locator):
        split_text = locator.split (separator)
        return split_text

